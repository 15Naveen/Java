import java.util.Arrays;
import java.util.List;

public class Main {
    public static void main(String[] args) {
        List<Integer> numbers = Arrays.asList(15, 42, 7, 89, 23, 56, 12, 98);

        // Using stream to filter and print 2-digit numbers
        numbers.stream()
               .filter(num -> num >= 10 && num < 100)
               .forEach(System.out::println);
    }
}
